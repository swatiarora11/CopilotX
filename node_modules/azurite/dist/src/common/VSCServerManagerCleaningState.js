"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class VSCServerManagerCleaningState {
    async start(serverManager) {
        throw new Error(`Cannot start server. ${serverManager.name} is cleaning.`);
    }
    async close(serverManager) {
        throw new Error(`Cannot close server. ${serverManager.name} is cleaning.`);
    }
    async clean(serverManager) {
        throw new Error(`Cannot clean server. ${serverManager.name} is cleaning.`);
    }
}
exports.default = VSCServerManagerCleaningState;
//# sourceMappingURL=VSCServerManagerCleaningState.js.map