"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class VSCServerManagerClosingState {
    async start(serverManager) {
        throw new Error(`Cannot start server. ${serverManager.name} is closing.`);
    }
    async close(serverManager) {
        throw new Error(`Cannot close server. ${serverManager.name} is closing.`);
    }
    async clean(serverManager) {
        throw new Error(`Cannot clean server. ${serverManager.name} is closing.`);
    }
}
exports.default = VSCServerManagerClosingState;
//# sourceMappingURL=VSCServerManagerClosingState.js.map